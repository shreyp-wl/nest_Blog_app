export const USER_CONSTANTS = {
  USER_SELECT_FIELDS: [
    "user.id",
    "user.email",
    "user.role",
    "user.userName",
    "user.firstName",
    "user.lastName",
  ],
  LOGIN_SELECT_FIELDS: ["user.id", "user.role"],
  REGISTER_SELECT_FIELDS: ["user.email", "user.password"],
  ID_SELECT_FIELDS: ["user.id"],
};
